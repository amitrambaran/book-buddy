import React, { Component } from 'react'

export default class upload extends Component {
  render() {
    return (
      <React.Fragment>
        <h1>Upload Page</h1>
        <p>Not to sure how to split this one (if necessary).</p>
        <p>Page to upload ebpubs and publish user written books.</p>
    </React.Fragment>
    )
  }
}
